import { PageScript } from "common/type"

export const initFightScreen: PageScript = ({ onGoNext, state }) => {
    const node = {
        userDispositionFieldNode: document.getElementById('userDispositionField'),
        enemyDispositionFieldNode: document.getElementById('enemyDispositionField')
    }


}